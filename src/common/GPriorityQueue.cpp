/*
 *  GPriorityQueue.cpp
 *  Phonosynth
 *
 *  Created by Greg Pascale on 7/30/08.
 *  Copyright 2008 __MyCompanyName__. All rights reserved.
 *
 */


/*

#include "GPriorityQueue.h"



template< typename K, typename V>
GPriorityQueue::GPriorityQueue<K, V>() :
	m_items.clear(),
	m_prioritizedItems.clear(),
	m_sorted(false)
{

}


template<typename K, typename V>
GPriorityQueue::~GPriorityQueue<K, V>() 
{
	
}


template<typename K, typename V>
void
GPriorityQueue::insert(const K& key, const V& value)
{	
	map<K, V*>::iterator iter;
	if((iter = m_items.find(key)) != m_items.end()){
		iter->
		
	m_prioritizedItems.push_back(GPtr<V>( &(m_items[key] = new V(value) );	
}

*/